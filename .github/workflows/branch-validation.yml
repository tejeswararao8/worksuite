name: Branch Validation

on:
  pull_request:
    branches:
      - development
      - main

jobs:
  validate-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Validate Branch Flow
        run: |
          BRANCH="${{ github.head_ref }}"
          TARGET="${{ github.base_ref }}"

          echo "Source: $BRANCH"
          echo "Target: $TARGET"

          # development -> main
          [[ "$BRANCH" == "development" && "$TARGET" == "main" ]] && exit 0

          # main -> development
          [[ "$BRANCH" == "main" && "$TARGET" == "development" ]] && exit 0

          # feature/* -> development
          [[ "$BRANCH" =~ ^feature\/.+$ && "$TARGET" == "development" ]] && exit 0

          # bug/* -> development
          [[ "$BRANCH" =~ ^bug\/.+$ && "$TARGET" == "development" ]] && exit 0

          # hotfix/* -> main
          [[ "$BRANCH" =~ ^hotfix\/.+$ && "$TARGET" == "main" ]] && exit 0

          # hotfix/* -> development
          [[ "$BRANCH" =~ ^hotfix\/.+$ && "$TARGET" == "development" ]] && exit 0

          echo "Invalid branch flow"
          exit 1
